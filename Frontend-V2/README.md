I refined around 70% of Allison’s mockup and turned it into a working version, especially the data preview based on our latest interview feedback with Janet. I added real functionality like file upload (CSV/XLSX), dynamic filters, pattern detection, and interactive graph components instead of static mockups. The prediction feature is partially implemented on the frontend, but it’s not fully accurate yet since the ML model is still in the training stage and hasn’t been fully integrated.
