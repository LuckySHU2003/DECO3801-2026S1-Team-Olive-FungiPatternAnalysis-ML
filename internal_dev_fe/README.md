# Internal Dev FE

Minimal plain HTML/JavaScript frontend for manually testing backend endpoints.

Run locally:

```bash
cd internal_dev_fe
python -m http.server 5173
```

Open http://localhost:5173.
